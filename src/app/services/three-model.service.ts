import { Injectable } from '@angular/core';

@Injectable({
  providedIn: 'root',
})
export class ThreeModelService {
  loadLandingModel(): void {
    // Three.js model loading will be added when 3D assets are introduced.
  }
}
