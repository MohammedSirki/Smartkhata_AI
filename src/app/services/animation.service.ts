import { Injectable } from '@angular/core';

@Injectable({
  providedIn: 'root',
})
export class AnimationService {
  initPageAnimations(): void {
    // Animation setup will be added when the visual design is implemented.
  }
}
